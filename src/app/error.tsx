"use client";

import { ErrorpageContainer } from "@/components/pages/public/ErrorpageContainer";

export default function Error(props: NextErrorPageProps) {
  return <ErrorpageContainer {...props} />;
}
